import { StyleSheet, Dimensions } from 'react-native';
import { title, color, CARD } from '../../config'
const { width, height } = Dimensions.get('window');

export default StyleSheet.create({});


















